{
  "name": "cmdx",
  "private": true,
  "scripts": {
    "lint": "pnpm -r run lint",
    "lint-staged": "./tools/node_modules/.bin/lint-staged",
    "test": "pnpm -r run test",
    "release": "./tools/node_modules/.bin/lerna publish",
    "prepare": "./tools/node_modules/.bin/husky"
  }
}
